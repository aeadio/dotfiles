- name: 'Configure World of Warcraft'
  hosts: 'localhost'
  vars:
    settings:
      - { key: 'disable-expose-fix', value: true, domain: 'com.blizzard.worldofwarcraft' }
  tasks:
    - include_role: 
        name: ../roles/macos-defaults
      loop: "{{ settings }}"
