# vim: ft=sh ts=2 sw=2 et:

() {
  setopt localoptions glob_assign glob_subst
  if [[ $OS != 'darwin' || $GUI != 'aqua' ]] return
  SSH_AUTH_SOCK=$HOME/Library/Containers/com.maxgoedjen.Secretive.SecretAgent/Data/socket.ssh(N)
  export SSH_AUTH_SOCK
}
