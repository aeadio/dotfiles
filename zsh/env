# vim: ft=sh ts=2 sw=2 et:

# At this point, $0 points to the ZSH binary
: ${ZSH:=${${:-${ZDOTDIR:-$HOME}/.zshenv}:A:h}}

# env.d
() {
  local f
  for f in $ZSH/env.d/*(e:'[[ -x $REPLY ]]':onN); source $f
}
