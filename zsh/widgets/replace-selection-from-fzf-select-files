# vim: ft=zsh ts=2 sw=2 et:

emulate -L zsh -o prompt_subst

local x y selection
get-selection | read -r selection

local matches
fzf-select-files $selection | read -r matches

if [[ -n $matches ]]; then
  replace-selection $matches
fi
