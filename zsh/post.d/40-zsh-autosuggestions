# vim: ft=sh ts=2 sw=2 et:

# Manually rebind zsh-autosuggestions widgets since we may have munged them in
# rc.d/zle.
if (( $+functions[_zsh_autosuggest_bind_widgets] )); then
  _zsh_autosuggest_bind_widgets
fi
